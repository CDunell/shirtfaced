# Renderer live enablement

Do not enable live generation until the deployment smoke is green. Then install the Gemini key, review budgets, enable Google media and run only the five benchmark scenes first.
