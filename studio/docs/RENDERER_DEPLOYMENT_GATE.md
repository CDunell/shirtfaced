# Renderer deployment gate

Phase 1 deploy is allowed with Google media disabled. The deployment must expose only the read-only validation manifest and scene packages. Live billable rendering requires a later deliberate configuration step with a Gemini key, enabled feature switch and reviewed spend ceilings.
