# Renderer validation entrypoint

Use `GET /api/renderer/validation` to inspect the five-scene harness and current budget/candidate policy. Use `GET /api/renderer/validation/{scene_id}` for the production package of one benchmark scene. These endpoints are intentionally non-billable.
