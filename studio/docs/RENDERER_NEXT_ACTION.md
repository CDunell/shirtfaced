# Renderer next action

After Phase 1 deploy is green, configure a Gemini API key and deliberately enable Google media for the bounded five-scene experiment. Do not expose general-purpose billable generation until attempt persistence and cost capture are wired through the existing generation-attempt model.
