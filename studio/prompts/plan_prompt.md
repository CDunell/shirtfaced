# Prompt Planning Template

Return only data matching the supplied PromptPlan JSON schema.

Use the World canon as binding.
Use recent continuity as context.
Use the selected shot as the assignment.
Do not change the selected hero product or camera position without reporting a validation error.
Write one production-ready image prompt.
