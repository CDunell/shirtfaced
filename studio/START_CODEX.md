# Start Codex Here

Open this repository in Codex and send:

```text
Read AGENTS.md and every document listed under its required reading order.

Then inspect the repository and implement Shirtfaced Studio according to the
specification.

Work in vertical slices. Begin with repository scaffolding, configuration,
database models and read-only loading/validation of WORLD.md, CONTINUITY.md
and SHOTLIST.md.

Do not call the live OpenAI API in tests. Do not skip tests. Do not introduce
React, a multi-agent framework, cloud infrastructure or unrelated features.

Continue until the first complete vertical slice is working and tested, then
report:
1. files changed;
2. behaviour implemented;
3. commands run;
4. exact test results;
5. remaining implementation phases.
```

For later sessions use:

```text
Read AGENTS.md, inspect current repository state and continue the next
unfinished phase in docs/IMPLEMENTATION_PLAN.md. Preserve all existing
decisions and run the full test suite before reporting completion.
```
