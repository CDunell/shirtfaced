name: W01-P28 derive C/D master-pixel fallback

on:
  push:
    branches: [main]
    paths:
      - "studio/coverage-cd-fallback-triggers/*.json"

permissions:
  contents: write

jobs:
  derive:
    runs-on: ubuntu-latest
    concurrency:
      group: w01-p28-derive-cd-fallback
      cancel-in-progress: false
    steps:
      - uses: actions/checkout@v4
      - name: Set up deploy key
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.ORACLE_DEPLOY_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -H "${{ secrets.ORACLE_HOST }}" >> ~/.ssh/known_hosts
      - name: Derive C/D from exact approved master pixels
        run: |
          set -euo pipefail
          ssh -i ~/.ssh/deploy_key ubuntu@${{ secrets.ORACLE_HOST }} 'bash -s' <<'REMOTE'
          set -euo pipefail
          cd /home/ubuntu/shirtfaced-studio
          set -a
          . .env
          set +a
          ./.venv/bin/python - <<'PY'
          import json
          from pathlib import Path
          from app.adapters.asset_store import FilesystemAssetStore
          from app.config import get_settings
          from app.db.session import get_session_factory
          from app.db.visual_models import VisualAsset
          from app.services.coverage_library import derive_coverage_frame
          from app.services.reference_resolution import resolve_scene_master

          EXPECTED = "7fd91e38bc021298cc563f0c41e95d89e2ca5ef995bcb22eaa430258a2376e88"
          settings = get_settings()
          store = FilesystemAssetStore(settings.assets_root_resolved)
          out = Path("var/diagnostics/w01-p28-derived-cd")
          out.mkdir(parents=True, exist_ok=True)

          with get_session_factory()() as session:
              master = resolve_scene_master(session, store, scene_key="W01-P28")
              if master.sha256 != EXPECTED:
                  raise SystemExit(f"Approved master changed: expected {EXPECTED}, got {master.sha256}. Nothing cut.")

              planned = [
                  {
                      "name": "w01-p28-c-emma-brock-crowd",
                      "x": 816,
                      "y": 0,
                      "height": 768,
                      "notes": "Provider/contact-sheet fallback C: exact master-pixel bar-side observation containing the established Emma/Brock region and surrounding crowd; human review required before Veo.",
                      "file": "C-emma-brock-crowd.png",
                  },
                  {
                      "name": "w01-p28-d-band-room-energy",
                      "x": 700,
                      "y": 0,
                      "height": 768,
                      "notes": "Provider/contact-sheet fallback D: exact master-pixel stage-side observation with band source, crowd shoulders and raised drinks; human review required before Veo.",
                      "file": "D-band-room-energy.png",
                  },
              ]
              results = []
              for shot in planned:
                  frame = derive_coverage_frame(
                      session,
                      store,
                      scene_key="W01-P28",
                      name=shot["name"],
                      x=shot["x"],
                      y=shot["y"],
                      height=shot["height"],
                      notes=shot["notes"],
                      actor="production-fallback",
                  )
                  session.flush()
                  asset = session.get(VisualAsset, frame.visual_asset_id)
                  if asset is None:
                      raise SystemExit(f"{shot['name']}: coverage asset missing after derive")
                  data = store.load(asset.storage_key)
                  (out / shot["file"]).write_bytes(data)
                  results.append({
                      "name": frame.name,
                      "coverage_frame_id": str(frame.id),
                      "visual_asset_id": str(frame.visual_asset_id),
                      "operation": frame.operation,
                      "x": frame.x,
                      "y": frame.y,
                      "width": frame.width,
                      "height": frame.height,
                      "frame_sha256": frame.frame_sha256,
                      "source_master_sha256": frame.source_master_sha256,
                      "approved_for_veo": frame.approved_for_veo,
                      "review_file": shot["file"],
                  })
              session.commit()

          (out / "result.json").write_text(json.dumps({
              "scene": "W01-P28",
              "provider_called": False,
              "master_sha256": EXPECTED,
              "human_review_required": True,
              "frames": results,
          }, indent=2, sort_keys=True), encoding="utf-8")
          PY
          REMOTE
          mkdir -p w01-p28-derived-cd
          scp -i ~/.ssh/deploy_key -r ubuntu@${{ secrets.ORACLE_HOST }}:/home/ubuntu/shirtfaced-studio/var/diagnostics/w01-p28-derived-cd/. w01-p28-derived-cd/
          test -s w01-p28-derived-cd/C-emma-brock-crowd.png
          test -s w01-p28-derived-cd/D-band-room-energy.png
          test -s w01-p28-derived-cd/result.json
          grep -q '"provider_called": false' w01-p28-derived-cd/result.json
          grep -q '"approved_for_veo": false' w01-p28-derived-cd/result.json
      - name: Upload C/D review artifact
        uses: actions/upload-artifact@v4
        with:
          name: w01-p28-derived-cd-${{ github.run_id }}
          path: w01-p28-derived-cd/
          if-no-files-found: error
          retention-days: 1
      - name: Record C/D derivation result and artifact run
        run: |
          set -euo pipefail
          git fetch origin main
          git checkout -B main origin/main
          mkdir -p studio/worlds/world-01/shots/diagnostics
          cp w01-p28-derived-cd/result.json studio/worlds/world-01/shots/diagnostics/W01-P28.derived-cd-result.json
          printf '{"run_id":%s,"artifact_name":"w01-p28-derived-cd-%s","provider_called":false}\n' '${{ github.run_id }}' '${{ github.run_id }}' > studio/worlds/world-01/shots/diagnostics/W01-P28.derived-cd-run.json
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add studio/worlds/world-01/shots/diagnostics/W01-P28.derived-cd-result.json studio/worlds/world-01/shots/diagnostics/W01-P28.derived-cd-run.json
          git commit -m "chore(W01-P28): record master-pixel C/D fallback [skip ci]"
          git push origin main
